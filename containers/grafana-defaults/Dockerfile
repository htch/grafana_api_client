FROM grafana/grafana:4.6.2

COPY grafana.ini /etc/grafana/grafana.ini

# docker build -t htch/grafana-defaults:latest .
# docker run -it --rm --name=grafana-defaults -p 3000:3000 htch/grafana-defaults:latest
# docker push htch/grafana-defaults:latest